import React from "react";

export default class LoginButton extends React.Component{
    render(){
        return(
            // retirms a button of the login
            <button variant="outlined">Submit Login</button>
        );
    }
}